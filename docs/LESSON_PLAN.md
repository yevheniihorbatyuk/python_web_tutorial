# 📋 Детальний Сценарій Заняття - Модуль 6

## Загальна Інформація

**Тема**: Реляційні бази даних та Асинхронне програмування
**Тривалість**: 3-4 години (з перервами)
**Формат**: Практичне заняття з демонстраціями та живим кодингом
**Рівень**: Intermediate

---

## 🎯 Цілі Заняття

Після заняття студенти зможуть:
- ✅ Пояснити як працює Event Loop в Python
- ✅ Написати асинхронний код для паралельних HTTP запитів
- ✅ Виміряти різницю в продуктивності sync vs async
- ✅ Писати SQL запити з JOIN, SUBQUERY, агрегацією
- ✅ Підняти PostgreSQL в Docker
- ✅ Підключитися до PostgreSQL з Python та виконувати CRUD операції
- ✅ Проводити аналіз даних через Jupyter Notebook

---

## 📅 Розклад Заняття

### ⏰ 0:00-0:15 - Вступ та Підготовка (15 хв)

**Мета**: Переконатись що всі готові до роботи

**Дії викладача**:
1. Привітання, перевірка присутності
2. Огляд теми заняття та цілей
3. Перевірка що у всіх:
   - Docker встановлено і запущено
   - Python 3.10+ встановлено
   - IDE/редактор готовий

**Практична частина**:
```bash
# Студенти виконують разом з викладачем:
cd python_web
docker --version
python --version
docker-compose up -d
docker-compose ps
```

**Контрольні питання**:
- Чи всі бачать зелений статус postgres?
- Хто має помилки?

---

## ЧАСТИНА 1: АСИНХРОННЕ ПРОГРАМУВАННЯ (90 хв)

### ⏰ 0:15-0:40 - Event Loop та Async Basics (25 хв)

**Мета**: Зрозуміти різницю між sync і async кодом

**Теорія (5 хв)**:
1. Що таке Event Loop?
   - Як виконується синхронний код (послідовно)
   - Як працює асинхронний код (паралельно)
   - Коли використовувати async?

2. Синтаксис:
   - `async def` - оголошення async функції
   - `await` - очікування async операції
   - `asyncio.gather()` - запуск кількох задач

**Демонстрація з кодом (15 хв)**:
```bash
python async_examples/01_async_basics.py
```

**Що показати**:
1. Синхронний код: 3 задачі по 2с = 6с загалом
2. Асинхронний код: 3 задачі по 2с = 2с загалом (прискорення 3x!)
3. Візуалізація Event Loop
4. Практичний приклад з завантаженням даних

**Інтерактив (5 хв)**:
- Хто може пояснити чому async швидший?
- Запитання від студентів
- Попросити когось пояснити своїми словами

**Завдання для студентів**:
```python
# Написати свою async функцію
async def download_file(filename, duration):
    print(f"Завантаження {filename}...")
    await asyncio.sleep(duration)
    print(f"{filename} готовий!")

# Запустити 5 завантажень паралельно
```

---

### ⏰ 0:40-1:20 - Async HTTP з aiohttp (40 хв)

**Мета**: Навчитись робити паралельні HTTP запити

**Теорія (5 хв)**:
1. Чому `requests` повільний для множинних запитів?
2. Як `aiohttp` вирішує цю проблему?
3. Реальні use cases:
   - Web scraping
   - API клієнти
   - Моніторинг множинних сервісів

**Демонстрація (25 хв)**:
```bash
python async_examples/02_async_http_client.py
```

**Що показати**:
1. Порівняння requests vs aiohttp на легких сайтах
2. Порівняння на важких сайтах (з затримкою)
3. Реальний приклад: GitHub API scraper
4. Додаткові можливості: POST, headers, timeout

**Акценти**:
- Синхронний підхід: 15 запитів = 15+ секунд
- Асинхронний: 15 запитів = 2-3 секунди
- Прискорення: 5-10x!

**Практична робота (10 хв)**:
```python
# Завдання: Написати async scraper для API
# 1. Завантажити дані з https://jsonplaceholder.typicode.com/posts
# 2. Завантажити дані з https://jsonplaceholder.typicode.com/users
# 3. Об'єднати результати
# 4. Виміряти час виконання
```

---

### ⏰ 1:20-1:30 - ПЕРЕРВА (10 хв)

---

### ⏰ 1:30-1:45 - WebSockets (Опціонально, 15 хв)

**Мета**: Показати коли використовувати WebSockets

**Теорія (5 хв)**:
- HTTP: request-response
- WebSocket: bidirectional, постійне з'єднання
- Use cases: чати, real-time апдейти, онлайн ігри

**Швидка демонстрація (10 хв)**:
```bash
python async_examples/03_websockets_demo.py
```

**Що показати**:
- WebSocket сервер
- Кілька клієнтів одночасно
- Broadcast повідомлень

**Примітка**: Це опціонально. Якщо час обмежений - можна пропустити.

---

## ЧАСТИНА 2: SQL ТА POSTGRESQL (90-120 хв)

### ⏰ 1:45-2:00 - Docker PostgreSQL (15 хв)

**Мета**: Переконатись що БД працює і всі можуть підключитись

**Демонстрація**:
```bash
# Перевірити статус
docker-compose ps

# Підключитись до БД
docker-compose exec postgres psql -U admin -d learning_db

# В psql виконати:
\dt                    # Показати таблиці
\d customers          # Структура таблиці
SELECT COUNT(*) FROM customers;
SELECT COUNT(*) FROM orders;
\q                    # Вийти
```

**Що пояснити**:
- Як працює docker-compose
- Як дані зберігаються у volumes
- Як підключитись ззовні (port 5432)

**Troubleshooting** (якщо у когось не працює):
- Перевірити логи: `docker-compose logs postgres`
- Перезапустити: `docker-compose restart postgres`
- Видалити volumes: `docker-compose down -v && docker-compose up -d`

---

### ⏰ 2:00-2:45 - Основи SQL (45 хв)

**Мета**: Опанувати базові та складні SQL запити

**Структура навчання**:

**1. SELECT та фільтрація (10 хв)**
```sql
-- Прості запити
SELECT * FROM customers LIMIT 5;
SELECT * FROM products WHERE price > 10000;
SELECT * FROM customers WHERE city IN ('Kyiv', 'Lviv');

-- Завдання: знайти всі товари дешевше 5000 грн
```

**2. Агрегатні функції (10 хв)**
```sql
-- COUNT, SUM, AVG, MIN, MAX
SELECT COUNT(*) FROM customers;
SELECT city, COUNT(*) FROM customers GROUP BY city;
SELECT AVG(price) FROM products;

-- HAVING - фільтрація після GROUP BY
SELECT city, COUNT(*) as count
FROM customers
GROUP BY city
HAVING COUNT(*) >= 2;

-- Завдання: порахувати середню ціну по категоріях
```

**3. JOIN (15 хв)**
```sql
-- INNER JOIN
SELECT c.first_name, c.last_name, o.total_amount
FROM customers c
INNER JOIN orders o ON c.id = o.customer_id;

-- LEFT JOIN (показати всіх клієнтів, навіть без замовлень)
SELECT
    c.first_name,
    COUNT(o.id) as order_count
FROM customers c
LEFT JOIN orders o ON c.id = o.customer_id
GROUP BY c.id, c.first_name;

-- Завдання: знайти всі замовлення з іменами клієнтів та співробітників
```

**4. SUBQUERY (10 хв)**
```sql
-- Товари дорожчі за середню ціну
SELECT name, price
FROM products
WHERE price > (SELECT AVG(price) FROM products);

-- Завдання: клієнти які витратили більше середнього
```

**Файл для практики**: `sql_examples/04_sql_examples.sql`

**Інтерактив**:
- Студенти виконують запити разом з викладачем
- Кожен запит обговорюємо: що він робить і навіщо
- Даємо короткі завдання після кожного блоку

---

### ⏰ 2:45-3:00 - ПЕРЕРВА (15 хв)

---

### ⏰ 3:00-3:30 - Python + PostgreSQL (30 хв)

**Мета**: Навчитись працювати з PostgreSQL через Python

**Демонстрація з кодом**:
```bash
python python_db/05_db_connection.py
```

**Що показати**:

**1. Підключення (5 хв)**
```python
import psycopg2

conn = psycopg2.connect(
    host="localhost",
    database="learning_db",
    user="admin",
    password="admin123"
)
```

**2. CRUD операції (10 хв)**
- CREATE: додати клієнта
- READ: прочитати дані
- UPDATE: оновити місто
- DELETE: видалити запис

**3. SQL Injection захист (5 хв)** ⚠️ ВАЖЛИВО!
```python
# ❌ НЕБЕЗПЕЧНО
query = f"SELECT * FROM users WHERE email = '{email}'"

# ✅ БЕЗПЕЧНО
query = "SELECT * FROM users WHERE email = %s"
cursor.execute(query, (email,))
```

**4. Транзакції (5 хв)**
```python
try:
    cursor.execute("UPDATE account SET balance = balance - 100 WHERE id = 1")
    cursor.execute("UPDATE account SET balance = balance + 100 WHERE id = 2")
    conn.commit()  # Зберегти
except:
    conn.rollback()  # Відкатити
```

**5. Context Managers (5 хв)**
```python
with get_db_connection() as conn:
    with conn.cursor() as cursor:
        cursor.execute("SELECT * FROM customers")
        # Автоматично закриється
```

**Практична робота**:
- Студенти модифікують код
- Додають нові CRUD операції
- Тестують різні запити

---

### ⏰ 3:30-4:00 - Jupyter Notebook Аналіз (30 хв)

**Мета**: Показати як аналізувати дані з PostgreSQL

**Демонстрація**:
```bash
# Конвертувати .py в .ipynb
jupytext --to notebook python_db/06_jupyter_db_operations.py

# Запустити Jupyter
jupyter notebook python_db/06_jupyter_db_operations.ipynb
```

**Що показати в notebook** (по черзі запускати cells):

1. **Підключення** (3 хв)
   - Імпорти
   - Підключення до БД
   - Завантаження даних у DataFrame

2. **Базовий аналіз** (5 хв)
   - head(), info(), describe()
   - value_counts() по містах
   - Статистика замовлень

3. **JOIN в pandas** (5 хв)
   - Об'єднання таблиць
   - Агрегація даних
   - Топ клієнтів

4. **RFM Аналіз** (10 хв)
   - Що таке RFM?
     - R - Recency (як давно купляв)
     - F - Frequency (як часто)
     - M - Monetary (скільки витрачає)
   - Сегментація клієнтів: VIP, Loyal, Potential, At Risk
   - Практичне застосування

5. **Візуалізація** (5 хв)
   - Графік продажів
   - Розподіл клієнтів
   - Категорії товарів

**Інтерактив**:
- Студенти запускають cells разом з викладачем
- Обговорюємо кожен результат
- Запитання та відповіді

---

## 🎯 Завершення Заняття (10-15 хв)

### Підсумок

**Що ми вивчили**:

1. **Асинхронне програмування**:
   - Event Loop та async/await
   - Паралельні HTTP запити з aiohttp
   - Прискорення в 5-10x
   - WebSockets для real-time

2. **SQL та PostgreSQL**:
   - Базові запити: SELECT, WHERE, ORDER BY
   - Агрегація: COUNT, SUM, AVG, GROUP BY
   - JOIN для об'єднання таблиць
   - SUBQUERY для складних запитів

3. **Python + PostgreSQL**:
   - Підключення через psycopg2
   - CRUD операції
   - SQL Injection захист
   - Транзакції

4. **Аналіз даних**:
   - Pandas + PostgreSQL
   - RFM сегментація
   - Візуалізація

### Домашнє завдання (опціонально)

1. **Async web scraper**:
   - Завантажити дані з 10+ API endpoints
   - Зберегти у JSON
   - Порівняти час sync vs async

2. **SQL задачі**:
   - Розв'язати 5 задач з https://datalemur.com/
   - Рівень: Medium

3. **Аналіз власних даних**:
   - Створити свою БД з 3+ таблицями
   - Завантажити дані
   - Провести аналіз через Jupyter

4. **Проект** (складний):
   - Async API клієнт для збору даних
   - Збереження в PostgreSQL
   - Dashboard з візуалізацією

### Питання та Відповіді (5 хв)

- Відповісти на запитання студентів
- Дати поради щодо подальшого навчання
- Рекомендовані ресурси

### Рефлексія (5 хв)

**Запитати студентів**:
- Що було найцікавіше?
- Що найскладніше?
- Які є запитання?
- Що хочете вивчити глибше?

---

## 📚 Додаткові Ресурси

### Документація
- [asyncio](https://docs.python.org/3/library/asyncio.html)
- [aiohttp](https://docs.aiohttp.org/)
- [PostgreSQL Tutorial](https://www.postgresql.org/docs/current/tutorial.html)
- [psycopg2](https://www.psycopg.org/docs/)

### Практика SQL
- [DataLemur](https://datalemur.com/) - SQL задачі від реальних компаній
- [SQLZoo](https://sqlzoo.net/) - інтерактивні уроки
- [LeetCode Database](https://leetcode.com/problemset/database/) - задачі різної складності

### Статті
- [Async IO in Python](https://realpython.com/async-io-python/)
- [PostgreSQL Performance Tips](https://www.postgresql.org/docs/current/performance-tips.html)

---

## 🎓 Поради Викладачу

### Підготовка
1. ✅ Протестувати всі скрипти за день до заняття
2. ✅ Переконатись що Docker працює
3. ✅ Підготувати запасний план якщо щось не працює
4. ✅ Мати список студентів для перевірки

### Під час заняття
- 👍 Робіть паузи кожні 45-60 хвилин
- 👍 Запитуйте чи все зрозуміло
- 👍 Заохочуйте задавати питання
- 👍 Показуйте реальні приклади використання
- 👍 Дозволяйте студентам експериментувати

### Troubleshooting
- 🔧 Docker не запускається → перевірити чи запущено Docker Desktop
- 🔧 Порт зайнятий → змінити в .env
- 🔧 Python помилки → перевірити venv та requirements.txt
- 🔧 Повільний інтернет → мати локальні копії даних

### Адаптація до часу
- ⏱️ Якщо часу мало: пропустити WebSockets та складні SQL запити
- ⏱️ Якщо часу багато: додати більше практичних завдань
- ⏱️ Орієнтуватись на темп групи

---

**Успішного заняття! 🚀**
